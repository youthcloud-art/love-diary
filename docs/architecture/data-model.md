# 数据模型

- `User`：账号、昵称、邮箱密码或微信 openid
- `Space`：一本双人共享日记
- `Membership`：用户与空间关系，包含 owner / partner 角色
- `Invite`：短期、单次使用的邀请码
- `Entry`：日记正文、日期、心情、作者和可见性
- `Media`：上传图片及缩略图，可绑定到一篇日记

主要约束：一个用户只能加入一个空间；一个空间最多两位成员；私密日记只有作者可读；媒体只能绑定到同一空间内的日记。
